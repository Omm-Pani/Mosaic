# =================================================================================
# File: docker/templates/pnpm-lock.yaml
# Purpose: Ensures reproducible builds by locking dependency versions.
# Note: This is a placeholder. A real file would be generated by running
# `pnpm install` once with the package.json above.
# =================================================================================
lockfileVersion: 5.4

specifiers:
  '@google/generative-ai': ^0.14.1
  # ... (and all other dependencies)

dependencies:
  '@google/generative-ai': 0.14.1
  # ...

devDependencies:
  # ...
